/**
 * Normaliza un texto determinado convirtiéndolo a minúsculas.
 *
 * @param {string} text
 * El texto a normalizar.
 *
 * @returns {string}
 * El texto normalizado en minúsculas.
 */
export function normalize(text: string) {
  return text.toLowerCase();
}
